info:
  name: Join Group
  type: http
  seq: 9

http:
  method: POST
  url: "{{BACKEND_URL}}/api/groups/join"
  headers:
    - name: Authorization
      value: JWT {{JWT_TOKEN}}
  body:
    type: json
    data: |-
      {
        "group_id":"6c05935f-cb83-49fe-8ce7-94b687bfa8ee"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
