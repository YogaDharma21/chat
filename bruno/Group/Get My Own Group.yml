info:
  name: Get My Own Group
  type: http
  seq: 6

http:
  method: GET
  url: "{{BACKEND_URL}}/api/own-groups"
  headers:
    - name: authorization
      value: JWT {{JWT_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
