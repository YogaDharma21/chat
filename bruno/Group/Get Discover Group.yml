info:
  name: Get Discover Group
  type: http
  seq: 4

http:
  method: GET
  url: "{{BACKEND_URL}}/api/groups"
  headers:
    - name: authorization
      value: JWT {{JWT_TOKEN}}
  params:
    - name: name
      value: update
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
