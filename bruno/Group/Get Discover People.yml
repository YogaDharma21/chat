info:
  name: Get Discover People
  type: http
  seq: 5

http:
  method: GET
  url: "{{BACKEND_URL}}/api/peoples"
  headers:
    - name: authorization
      value: JWT {{JWT_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
