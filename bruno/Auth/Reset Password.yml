info:
  name: Reset Password
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BACKEND_URL}}/api/auth/reset-password"
  body:
    type: json
    data: '{"password":"testtesttest","confirmPassword":"testtesttest"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
