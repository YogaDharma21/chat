info:
  name: Get Room Personal
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_URL}}/api/chat/rooms"
  headers:
    - name: Authorization
      value: JWT {{JWT_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
