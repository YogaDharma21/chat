info:
  name: Get Balance
  type: http
  seq: 3

http:
  method: GET
  url: "{{BACKEND_URL}}/api/balance"
  headers:
    - name: Authorization
      value: JWT {{JWT_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
