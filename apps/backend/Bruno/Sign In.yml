info:
  name: Sign In
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/auth/sign-in
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: test@test.test
      - name: password
        type: text
        value: testajasihinia
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
